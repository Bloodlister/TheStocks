{% extends 'base.html.twig' %}

{% dump(item) %}

{% block body %}

    <div class="container">
        <div class="row">
            <div class="col-md-offset-3 col-md-6">
                {{ form_start(form, { 'attr': { 'class': 'panel panel-default' } }) }}

                <div class="panel-body">
                    {{ form_widget(form) }}
                </div>

                {{ form_end(form) }}
            </div>
        </div>
    </div>

{% endblock %}